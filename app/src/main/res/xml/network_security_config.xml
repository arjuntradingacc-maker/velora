<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production traffic is TLS-only; certificate pinning is applied per-host
         in NetworkModule via OkHttp's CertificatePinner rather than statically
         here, so pins can rotate without an app release. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
